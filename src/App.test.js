import React from 'react';

// Basic smoke test - verifies React is working
test('React is defined', () => {
  expect(React).toBeDefined();
});

test('Environment is set up correctly', () => {
  expect(true).toBe(true);
});
